<div id="hello" align="center">
  <h1>
    <img src="https://i.pinimg.com/1200x/ec/d0/19/ecd019cb60258acf2728fd0739bd803f.jpg"/>
  </h1>
</div>

# 🔏Конфиденциальный мессенджер на базе LoRa и BLE
__________________________________________________________________________
### 🛑Hollow: 
> #### Когда интернет молчит — мы говорим.

>Названия
>1. **MeshCast** — mesh-сеть + broadcast;
>2. **AirLink** — акцент на беспроводной связи без инфраструктуры;
>3. **Wavecast** — трансляция волн;
>4. **WayPoint** — путевая точка, термин из навигации;
>5. **DriftWave** — дрейфующая волна;
>6. ==**Hollow**== — пустота, работает в пустоте без инфраструктуры;
>7. OffGrid — прямое «вне сети».
>8. DarkWire — «тёмный провод», намёк на автономность и приватность.
>9. Bastion — «бастион», ассоциация с устойчивостью.
# ❕Основная информация
__________________________________________________________________________
>Основной стек технологий
>1. **==Rust==** - как основной язык для бэк-энда и бизнес-логики;
>2. **==Slint==** - как основной фреймворк для разработки нативного приложение (кроссплатформенность: MacOS, iOS, Android, Linux, Windows). Без поддержки x32;
>3. **==BLE==** - как одна из технологий передачи данных: фото, видео, голосовые сообщения, кружки(?). Для текста более чем: 450 символов (латиница), 200 символов (кириллица), 325 символов (смешенные).
>4. **==LoRa==** - как одна из технологий передачи данных на дальние расстояния. Предпочтительно SF10 и для сообщений не более: 450 символов (латиница, 1 байт на символ), 200 символа (кириллица, 2 байта на символ),  325 символов (смешенные 1-2 байта на символ).
>5. **==Noise==** - фреймворк для построения криптографический протоколов. Для групповых сообщений в mesh-сети нужен отдельный механизм поверх — например, предварительно согласованный групповой ключ или MLS (Messaging Layer Security).

>  Схема отправки личных сообщений и сообщений группы
>```mermaid
>  flowchart LR
>  User["Пользователь печатает сообщение."] --> Check{"Проверка сообщения."}
>  Check --> |"Проверка Markdown"| CheckA{"Поддерживается?"}
>  Check --> |"Проверка размера"| CheckB{"Проходит?"}
>  CheckA --> |"Да
>  Проверка размера"| CheckB
>  CheckA --> |"Нет"| Error1["Ошибка: не поддерживаемый формат текста!"]
>  CheckB --> |"Да"| Encrypt["Шифрование данных с помощью Noise."]
>  CheckB --> |"Нет"| Error2["Ошибка: слишком большой объем данных!"]
>  Encrypt --> CheckC{"Подходит для LoRa?"}
>  CheckC --> |"Да"| LoRa["Передача с помощью Lora. Режим SF10."]
>  CheckC --> |"Нет"| BLE["Передача с помощью BLE (buetooth)."]
>  CheckD{"Получатель в сети?
>   Получатель в зоне доступности?"}
>  LoRa --> CheckD
>  BLE --> CheckD
>  CheckD --> |"Да"| Result["Сообщение доставлено!"]
>  CheckD --> |"Нет"| Store["Сохранить локально."]
>  Store --> Wait["Ожидание получателя."]
>  Wait --> Result
>```

>Схема отправки сообщений через BLE посредством mesh-сети
>```mermaid
>flowchart LR
>Bob["Bob"] --> |"BLE 30 м"| Alice["Alice"]
>Bob --> |"Сообщение, отправленное Дэйву, проходит через mesh-сеть."| Alice
>Alice --> |"BLE 30 м"| Carl["Carl"]
>Alice --> Carl
>Carl --> |"BLE 30 м"| Dave["Dave"]
>Carl --> Dave
>```

>Примерный список возможностей для реализации
>#### Это не все функции, но основные. В будущем будут добавлены новые возможности.
>>Не сделаны:<br>
>>`/j` @channel `- Присоединиться или создать канал`<br>
>>`/m` @name `message - Отправить личное сообщение`<br>
>>`/w - Показать всех пользователей онлайн (В зоне доступа)`<br>
>>`/channels - Показать все обнаруженные рядом каналы`<br>
>>`/block` @name `- Блокировать юзера`<br>
>>`/block - Показать всех блокированных юзеров`<br>
>>`/unblock` @name `- Разблокировать юзера`<br>
>>`/clear - Очистить чат`<br>
>>`/passch` @password  `- Установить/Изменить пароль канала (владелец)`<br>
>>`/transfer` @name `- Передать владение каналом (владелец)`<br>
>>`/cg` @group @password `- Создать новую группу (Название и пароль)`<br>
>>`/passgr` @password `- Изменить пароль группы (Владелец)`<br>
>>`/ig` @group #name `- Пригласить пользователя в группу`<br>
>>`/nwth` @thread `- Создать новую ветку обсуждений в группе`<br>
>>`/kg` @group @name `- Выгнать участника из группы`<br>
>>`/bg` @group @name `- Заблокировать участника в группе`<br>
>>## To be continued...
>
>>Сделаны:
>>### Пустовато тут...

# ❓FAQ
__________________________________________________________________________
### Что такое Hollow?
> Hollow — конфиденциальный мессенджер, который работает **без интернета**, используя радио (LoRa) и Bluetooth Low Energy (BLE). Он ориентирован на текстовый обмен и локальные сети людей в условиях отсутствия сотовой связи и Wi‑Fi.

### В чём ключевая идея Hollow?
> Основная идея: «**Когда интернет молчит — мы говорим.**» Hollow строит связь от устройства к устройству (mesh/ретрансляция), без операторов и централизованных серверов, с end‑to‑end шифрованием.

### Почему именно LoRa и BLE?
>- **BLE** — высокая скорость на малых расстояниях, подходит для:
>     - фото, видео, голосовых сообщений, кружков,
>     - быстрых текстовых чатов «рядом».
>- **LoRa** — очень малая скорость, но большая дальность, подходит для
>     - коротких сообщений на километры,
>     - работы в мёртвых зонах и оффлайн‑сценариях.
>- В будущем возможно добавление **Wi-Fi Direct**
